@model List<Vidly.Models.Movie>
<h2>Movies</h2>
<table class="table">
    <thead>
        <tr>
            <th scope="row">Movies</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var movie in Model)
        {
            <tr>
                <td>
                    @movie.Name
                </td>
            </tr>
          }
    </tbody>

</table>
